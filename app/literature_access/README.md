# literature_access service
Start only this service:
```bash
sudo docker compose up -d --build literature-access
```
Server starts at
(http://0.0.0.0:8000)

Documentation at
(http://0.0.0.0:8000/docs)
