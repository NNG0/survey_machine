# Debug interface

A sinple debug interface I wrote to debug the state and potentially connect to the database later.

If [cargo](https://rustup.rs/) is already installed, it can be started with:

```shell
cargo run
```
